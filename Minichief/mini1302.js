function max(a,b,c){
    
    var result ="";
    
    if(isNaN(a) || isNaN(b)|| isNaN(c)){
        console.log("Error,a,b,c must be a number ");
    }else{
        return result =Math.max(a,b,c);
    }
}